# 翻译系统使用指南

## 📖 系统概览

现在的翻译系统已经**自动集成了智能格式化流程**，翻译完成后会自动进行格式清理和排版优化。
Web 端任务则改成了**SQLite 队列 + worker 认领执行**，不再依赖请求生命周期内直接跑完整翻译。
主翻译链现在还加入了**三角色多 agent 协作**：文档分析员、主翻译员、审校修复员。

---

## 🚀 快速开始

### 基本用法

```bash
# 翻译 Markdown 文件
python translate.py input/your_book.md

# 翻译 PDF 文件
python translate.py BookTrans/your_book.pdf

# 翻译 EPUB 文件
python translate.py BookTrans/your_book.epub
```

### 指定输出路径

```bash
python translate.py input/book.md -o output/book_CN.md
```

### 使用术语表

```bash
python translate.py data/input/book.md -g data/glossaries/CPglossary.json
```

### 双语对照输出

```bash
python translate.py data/input/book.md --skip-conversion --bilingual
```

### 快速运行测试

```bash
# 后端语法检查 + 回归测试
bash scripts/run_tests.sh

# 额外包含前端构建检查
bash scripts/run_tests.sh --with-frontend
```

### Web 模式启动

```bash
# 推荐：长任务模式（后台 API + 独立 worker + 防休眠）
bash scripts/longrun.sh

# 默认：API + 内联 worker
python run_server.py

# 或拆成两个进程
TRANSLATION_INLINE_WORKER=0 python run_server.py
python run_worker.py

# 多进程扩容
python run_worker.py --processes 4 --parallel-tasks 2
```

说明：
- `bash scripts/longrun.sh` 是最适合长文档翻译的启动方式
- 它会在后台启动 API 和独立 worker
- 日志默认写到 `logs/server.out` 和 `logs/worker.out`

如果你想显式防止 Mac 休眠，可以这样启动：

```bash
TRANSLATION_PREVENT_SLEEP=1 bash scripts/longrun.sh
```

### 跳过格式化（如果你不想自动格式化）

```bash
python translate.py input/book.md --skip-formatting
```

---

## 🎨 自动格式化功能

翻译完成后，系统会**自动执行**以下格式化步骤：

### ✅ 保护功能
1. **代码块保留** - 所有 ``` 代码块和行内 `代码` 完整保留
2. **目录保留** - 自动识别 `::: tableofcontents` 并清理目录结构
3. **块级结构解析** - 按标题、列表、段落、Pandoc div 分块处理，减少误清洗

### ✅ 转换功能
4. **标题标准化** - 移除 Pandoc 残留标记
   ```markdown
   # []{#id}标题 {.class} → # 标题
   ```

5. **引用块转换** - 将 Pandoc 引用转为标准 Markdown
   ```markdown
   ::: quote
   文本
   :::
   →
   > 文本
   ```

6. **强调转换** - 转换语义标记
   ```markdown
   [重点]{.emphasis} → **重点**
   [斜体]{.italic} → *斜体*
   ```

7. **图片路径修复** - 绝对路径转相对路径
   ```markdown
   ![](/Users/.../images/pic.jpg) → ![pic](images/pic.jpg)
   ```

### ✅ 清理功能
8. **删除 HTML 注释** - `<!--l. 59-->`{=html}
9. **删除锚点** - `[]{#ANickLandReader_split_000.html}`
10. **删除 Calibre 类** - `{.calibre1}`, `{.ecrm}`

### ✅ 排版优化
11. **书籍排版美化**
    - 章节标题前后自动添加空行
    - 优化段落间距
    - 统一列表格式

---

## 📊 翻译流程详解

### 完整流程

```
输入文件 (PDF/EPUB/MD)
   ↓
步骤 1: 文档转换 (如果需要)
   ↓
步骤 2: 读取文件内容
   ↓
步骤 3: 加载术语表
   ↓
步骤 4: 初始化翻译引擎
   ↓
步骤 5: 章节感知分块
   ↓
步骤 6: 异步并发翻译
   ├─ 文档分析员生成全局摘要 / 风格 / 术语提示
   ├─ Chunk 元数据与上下文
   ├─ 术语表约束
   ├─ 主翻译员执行 chunk 翻译
   ├─ 质量校验
   ├─ Chunk 缓存命中恢复
   └─ 审校修复员处理高风险块
   ↓
步骤 7: 顺序写出
   ↓
步骤 8: 智能格式化清理
   ↓
步骤 9: 完成
```

### 格式化处理细节

翻译完成后，系统会：

1. **构建块级结构** - 识别标题、代码块、Pandoc div、列表和段落
2. **按节点格式化** - 只对非代码节点做清理和转换
3. **渲染回 Markdown** - 保持块边界和段落间距
4. **排版优化** - 调整标题和段落间距

---

## 📁 输出文件位置

### 默认输出

```
data/output/{文件名}_CN.md
```

### 自定义输出

```bash
python translate.py input/book.md -o my_output/translated.md
```

---

## 🔧 命令行参数详解

```bash
python translate.py <输入文件> [选项]

必需参数:
  <输入文件>           输入文件路径 (PDF/EPUB/Markdown)

可选参数:
  -o, --output         输出文件路径
                       默认: data/output/{filename}_CN.md

  -g, --glossary       术语表路径
                       默认: data/glossaries/glossary.json

  --skip-conversion    跳过格式转换 (输入已是 Markdown)

  --skip-formatting    跳过格式化后处理
                       保留原始翻译结果 (不推荐)

  --bilingual          双语对照模式
                       输出原文引用块 + 中文译文

  -c, --config         配置文件路径
                       默认: config/config.yaml
```

---

## 💡 使用示例

### 示例 1: 翻译带术语表的哲学文本

```bash
python translate.py BookTrans/Cyclonopedia.epub \
  -g CPglossary.json \
  -o output_final/Cyclonopedia_CN.md
```

**说明**:
- 使用 Cyclonopedia 专用术语表 (73 个术语)
- 自动转换 EPUB → Markdown
- 翻译后自动格式化
- 输出到 `output_final` 目录

### 示例 2: 翻译已有的 Markdown 文件

```bash
python translate.py input/NickLandWritings.md --skip-conversion
```

**说明**:
- 跳过格式转换 (输入已经是 Markdown)
- 使用默认术语表 (如果存在)
- 自动格式化

### 示例 3: 翻译但不格式化

```bash
python translate.py input/test.md --skip-formatting
```

**说明**:
- 保留原始翻译输出
- 不执行格式化清理
- 适合调试或特殊需求

### 示例 4: 批量翻译多个文件

```bash
#!/bin/bash
for file in input/*.md; do
  python translate.py "$file" -g glossary.json
done
```

---

## 📈 日志和进度

### 实时日志

翻译过程中，系统会输出详细日志：

```
============================================================
🚀 翻译流水线启动
============================================================
📄 使用 Markdown 文件: input/book.md
📖 读取文件: input/book.md
📚 加载术语表: 73 个词条
⚙️ 初始化翻译引擎...
✂️ 文本分块完成: 150 个块
============================================================
🚀 开始翻译...
============================================================
✅ Chunk 0 完成 (1/150, 0.7%, 速度: 12.5 chunks/分钟)
✅ Chunk 1 完成 (2/150, 1.3%, 速度: 13.2 chunks/分钟)
...
============================================================
🎨 开始格式化处理...
📝 读取翻译结果...
🔧 应用智能格式化...
💾 保存格式化结果...
🎨 格式化完成:
  ✅ 标题标准化: 45
  ✅ 引用块转换: 23
  ✅ 强调转换: 128
  ✅ 代码块保留: 67
  ✅ 目录保留: 1
  ✅ 图片修复: 8
  ✅ 锚点移除: 342
============================================================
✅ 翻译完成!
⏱️  总耗时: 18.50 分钟
📈 平均速度: 8.1 chunks/分钟
✅ 成功: 150 个
❌ 失败: 0 个
💾 输出文件: data/output/book_CN.md
============================================================
```

### 日志文件

详细日志保存在:
```
logs/translation.log
```

---

## ⚠️ 常见问题

### Q1: 代码块被破坏了怎么办？

**A**: 现在已经不会了！新版本会自动保护所有代码块：
- ``` 多行代码块
- `行内代码`

### Q2: 目录被删除了？

**A**: 新版本会自动识别并保留目录，同时清理 Pandoc 残留标记。

### Q3: 如果我不想自动格式化呢？

**A**: 使用 `--skip-formatting` 参数:
```bash
python translate.py input.md --skip-formatting
```

### Q4: 能单独对已翻译文件格式化吗？

**A**: 可以！使用独立的格式化脚本:
```bash
python scripts/smart_markdown_formatter.py output/book_CN.md
```

### Q5: 格式化会修改我的原文吗？

**A**: 不会。格式化只处理输出文件 (`*_CN.md`)，不会修改输入文件。

### Q6: 任务失败后还能拿到结果吗？

**A**: 视失败范围而定：
- 如果只有部分 chunk 失败，系统会生成带失败占位符的结果文件，Web 端状态为 `partial_success`
- 如果全部 chunk 都失败，任务状态为 `failed`，不会提供结果下载

### Q7: 现在支持断点续传吗？

**A**: 当前主流程已经支持**基于 chunk 缓存的恢复式重跑**。中途中断后，重新运行同一命令会优先复用已成功 chunk 的缓存，只继续处理未完成部分。它不是传统意义上的“任务快照回放”，但在绝大多数重跑场景下已经能显著减少重复请求。

### Q8: Web 端任务现在还是请求里直接跑吗？

**A**: 不是。现在是“任务入队 -> worker 认领 -> 执行翻译”的模式。默认 `run_server.py` 会启动一个内联 worker；如果你想把 API 和执行器拆开，只要设置 `TRANSLATION_INLINE_WORKER=0` 后再单独运行 `python run_worker.py`。如果需要更高吞吐，也可以直接启动多进程 worker：`python run_worker.py --processes 4 --parallel-tasks 2`。

### Q9: 多 agent 现在是怎么工作的？

**A**: 现在的主链不是“每个 chunk 都开三次模型调用”，而是更克制的三角色协作：
- 文档分析员先对整本文档采样，产出摘要、风格约束和术语提示
- 主翻译员在每个 chunk 翻译时吃这些全局上下文
- 审校修复员只在规则校验判定为高风险时才介入

这样能明显改善术语漂移和章节语气断裂，同时不会把成本放大成全量三倍调用。

---

## 🛠️ 高级配置

### 修改配置文件

编辑 `config/config.yaml`:

```yaml
# API 配置
api:
  model: "deepseek-ai/DeepSeek-V3"
  translator:
    temperature: 0.3

concurrency:
  max_concurrent_requests: 10
  rate_limit_per_minute: 200

text_splitting:
  chunk_size: 2000
  chunk_overlap: 200
  context_window: 800

quality:
  enable_qa_check: true
  max_fix_attempts: 1
  untranslated_word_span: 12
  max_glossary_checks: 25
```

### 自定义术语表

创建 JSON 格式术语表:

```json
{
  "Hyperstition": "超虚构 (Hyperstition)",
  "War Machine": "战争机器 (War Machine)",
  "Rhizome": "根茎 (Rhizome)"
}
```

使用:
```bash
python translate.py input.md -g my_glossary.json
```

---

## 📞 技术支持

### 遇到问题？

1. **查看日志** - `logs/translation.log`
2. **检查输出** - 对比原文和译文
3. **尝试重新运行** - 当前主流程会重新从头翻译

### 性能优化

**如果翻译速度慢**:
- 降低并发数: 修改 `config.yaml` 中的 `max_concurrent`
- 检查网络连接
- 查看 API 速率限制

**如果遇到 API 错误**:
- 检查 `.env` 文件中的 API 密钥
- 确认 API 额度充足
- 降低速率限制

---

## 🎯 最佳实践

### 1. 翻译前准备

✅ 准备好术语表 (针对专业文本)
✅ 检查输入文件格式正确
✅ 预估翻译时间 (约 10 chunks/分钟)

### 2. 翻译过程中

✅ 保持网络连接稳定
✅ 监控日志输出
✅ 尽量不要中断进程（当前主流程不会自动续跑）

### 3. 翻译后检查

✅ 检查目录是否完整
✅ 验证代码块格式
✅ 查看图片显示是否正常
✅ 核对专业术语翻译

### 4. 格式问题修复

如果格式还有问题，可以:

```bash
# 重新格式化
python scripts/smart_markdown_formatter.py output/book_CN.md

# 或使用暴力清理器 (仅纯文本)
python scripts/advanced_markdown_cleaner.py output/book_CN.md
```

---

## 📖 总结

### 核心改进

1. ✅ **自动化格式化** - 翻译完成后自动清理
2. ✅ **代码块保护** - 完整保留所有代码格式
3. ✅ **书籍排版优化** - 自动调整章节间距
4. ✅ **目录保留** - 智能识别并清理目录
5. ✅ **灵活控制** - 可选择跳过格式化
6. ✅ **双语导出** - 支持双语 Markdown / HTML 输出
7. ✅ **明确状态** - Web 端区分 `completed`、`partial_success`、`failed`

### 使用流程

```bash
# 一步完成翻译 + 格式化
python translate.py input/book.md -g glossary.json

# 查看结果
open data/output/book_CN.md
```

就这么简单！🎉

---

**更新时间**: 2026-01-16
**版本**: v3.1
