# 📚 Markdown 格式化工具使用指南

## 🎯 工具总览

我们创建了3个格式处理工具,从简单到智能:

| 工具 | 功能 | 适用场景 |
|------|------|---------|
| **advanced_markdown_cleaner.py** | 暴力清理,删除所有标记 | 只要纯文本,不在意排版 |
| **smart_markdown_formatter.py** | 智能转换,保留有用格式 | 需要保留目录和基本排版 |
| **auto_format_pipeline.sh** | 自动化流水线 | 一键处理所有步骤 |

---

## 🔍 格式标记的含义详解

### ❓ 这些标记本来是做什么的?

让我们通过例子理解:

```markdown
原始 EPUB/HTML 格式:
<h2 id="chapter1" class="calibre2 likechapterhead">第一章</h2>

Pandoc 转换后的 Markdown:
## []{#chapter1 .calibre2}第一章 {.likechapterhead}

应该转换为:
## 第一章
```

### 📋 标记类型对照表

| 标记 | 含义 | 原本用途 | 是否保留 |
|------|------|---------|----------|
| `{#id}` | **锚点ID** | HTML跳转链接 | ❌ 删除 (Markdown自动生成) |
| `{.calibre1~9}` | **Calibre样式类** | 阅读器字体/颜色 | ❌ 删除 (无意义序号) |
| `{.likechapterhead}` | **章节标题语义** | 标识这是章节 | ✅ 转换为 `#` |
| `{.quote}` | **引用块** | 引文样式 | ✅ 转换为 `>` |
| `{.center}` | **居中文本** | 文本居中 | ✅ 保留 (HTML) |
| `{.emphasis}` | **强调文本** | 加粗/斜体 | ✅ 转换为 `**text**` |
| `[text]{.ecrm}` | **字体标记** | Computer Modern字体 | ❌ 删除 |
| `<!--comment-->` | **HTML注释** | 行号、元数据 | ❌ 删除 |

---

## 🛠️ 工具使用方法

### 方案 1: 暴力清理 (简单快速)

**适用**: 你只要干净的纯文本,不在意任何排版

```bash
# 使用 advanced_markdown_cleaner.py
python scripts/advanced_markdown_cleaner.py input.md output.md

# 会删除:
# - 所有 HTML 注释
# - 所有锚点
# - 所有类名
# - 所有 Pandoc 指令
# - 目录 (!!!)
```

**优点**: 最干净,文件最小
**缺点**: 目录丢失,所有排版信息丢失

---

### 方案 2: 智能格式化 (推荐)

**适用**: 需要保留目录和基本排版结构

```bash
# 使用 smart_markdown_formatter.py
python scripts/smart_markdown_formatter.py input.md output.md

# 会做什么:
# ✅ 保留目录 (清理后)
# ✅ 标准化标题格式
# ✅ 转换引用块 (::: quote → >)
# ✅ 转换强调 ([text]{.emphasis} → **text**)
# ✅ 修复图片路径
# ✅ 删除冗余标记
```

**优点**: 保留有用信息,转换为标准Markdown
**缺点**: 处理速度稍慢

---

### 方案 3: 自动化流水线 (最完整)

**适用**: 一键处理所有后处理步骤

```bash
# 使用自动化脚本
./scripts/auto_format_pipeline.sh input.md output.md

# 执行步骤:
# 1. 智能格式化 (保留目录)
# 2. 修复图片路径
# 3. 最终清理 (sed处理)
```

**优点**: 一键完成所有处理
**缺点**: 需要bash环境

---

## 📊 效果对比

### 原始文件 (Pandoc 转换后)

```markdown
[]{#ANickLandReader_split_000.html}

`<!--l. 59-->`{=html}

![PIC](/Users/sena/Desktop/LLMAgent/translator/data/temp/NickLandWritings/images/cover.jpg){.calibre1}

## []{#ANickLandReader_split_001.html_x1-2000 .calibre2}目录拓扑学(Topology of Contents) {#ANickLandReader_split_001.html_calibre_pb_0 .likechapterhead}

::: tableofcontents
[ [导论](#ANickLandReader_split_001.html_Q1-1-3){.calibre3}]{.chaptertoc}\
[I  [资本与人工智能(Capital and AI)](#ANickLandReader_split_003.html_x1-5000I){.calibre4}]{.parttoc}\
:::
```

### 暴力清理后 (advanced_markdown_cleaner)

```markdown
!

## 目录拓扑学(Topology of Contents)

[[ ]]资本是机器性（非工具性）的全球化...
```

**问题**: 目录完全丢失!

### 智能格式化后 (smart_markdown_formatter)

```markdown
![cover](images/cover.jpg)

## 目录拓扑学(Topology of Contents)

> [ [导论](#ANickLandReader_split_001.html_Q1-1-3)]
> [I  [资本与人工智能(Capital and AI)](#ANickLandReader_split_003.html_x1-5000I)]
```

**改进**: ✅ 目录保留, ✅ 格式清爽, ✅ 图片路径修复

---

## 🎨 是否应该保留这些格式?

### ✅ 应该保留并转换的

**原因**: 这些标记包含有用的语义信息

| 原始标记 | 语义 | 应转换为 |
|---------|------|---------|
| `{.likechapterhead}` | 章节标题 | `# 标题` |
| `{.quote}` | 引用 | `> 引用文本` |
| `{.center}` | 居中 | `<center>文本</center>` |
| `[text]{.emphasis}` | 强调 | `**text**` |
| `[text]{.italic}` | 斜体 | `*text*` |

### ❌ 应该删除的

**原因**: 这些标记只是冗余的技术标签

- `{#ANickLandReader_split_000.html}` - 内部ID,无用
- `{.calibre1}` - Calibre序号,无意义
- `{.ecrm}` - 字体标记,Markdown不支持
- `<!--l. 59-->` - 行号注释,无用
- `\[\[ \]\]` - 转义错误

---

## 🔧 集成到自动化翻译流程

### 修改翻译引擎自动调用

编辑 `src/core/translator.py` 或 `src/api/services/translation_service.py`:

```python
async def start_translation(self, task_id: str):
    # ... 原有翻译逻辑 ...

    # 翻译完成后,自动格式化
    await self._auto_format_result(output_path)

async def _auto_format_result(self, file_path: Path):
    """自动格式化翻译结果"""
    from scripts.smart_markdown_formatter import SmartMarkdownFormatter

    # 读取翻译结果
    with open(file_path, 'r', encoding='utf-8') as f:
        content = f.read()

    # 智能格式化
    formatter = SmartMarkdownFormatter()
    formatted = formatter.format(content)

    # 覆盖写入
    with open(file_path, 'w', encoding='utf-8') as f:
        f.write(formatted)

    print(f"✅ 自动格式化完成: {file_path}")
```

### 完整的翻译+格式化流程

```bash
# 1. 翻译文件
python scripts/async_translator.py

# 2. (自动执行) 格式化清理
#    已集成到翻译引擎中

# 3. (可选) 手动再次格式化
python scripts/smart_markdown_formatter.py output.md final.md
```

---

## 📖 实际案例对比

### 案例: NickLandWritings.epub 翻译

| 指标 | 原始翻译 | 暴力清理 | 智能格式化 |
|------|---------|---------|-----------|
| 文件大小 | 652 KB | 445 KB (-32%) | 589 KB (-10%) |
| 目录 | ❌ 杂乱 | ❌ 丢失 | ✅ 保留 |
| 标题格式 | ❌ 带标记 | ✅ 干净 | ✅ 干净 |
| 引用块 | ❌ ::: 标记 | ❌ 删除 | ✅ 转换为 `>` |
| 图片 | ❌ 绝对路径 | ❌ 损坏 | ✅ 相对路径 |
| 可读性 | ⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

**推荐**: 使用 **智能格式化** 工具!

---

## 🚀 快速开始

### 一行命令处理你的翻译文件

```bash
# 智能格式化 (推荐)
python scripts/smart_markdown_formatter.py data/output/your_book_CN.md

# 或使用自动化流水线
./scripts/auto_format_pipeline.sh data/output/your_book_CN.md
```

### 批量处理多个文件

```bash
# 处理 output_final 目录下的所有文件
for file in output_final/*_CN.md; do
    echo "处理: $file"
    python scripts/smart_markdown_formatter.py "$file" "${file%.md}_formatted.md"
done
```

---

## 💡 最佳实践建议

### 1. 总是保留原始文件

```bash
# 不要覆盖原文件
python scripts/smart_markdown_formatter.py input.md input_formatted.md

# 而不是
python scripts/smart_markdown_formatter.py input.md input.md  # ❌ 危险
```

### 2. 先用智能格式化,必要时再暴力清理

```bash
# 步骤 1: 智能格式化
python scripts/smart_markdown_formatter.py input.md temp.md

# 步骤 2: 检查结果,如果还有问题...
python scripts/advanced_markdown_cleaner.py temp.md final.md
```

### 3. 目录很重要,尽量保留

目录提供了文档的导航结构,对于长篇文档非常重要。**智能格式化工具会保留目录**。

### 4. 根据用途选择工具

| 用途 | 推荐工具 |
|------|---------|
| 学术出版 | smart_markdown_formatter |
| 个人阅读 | advanced_markdown_cleaner |
| 网页发布 | smart_markdown_formatter |
| 纯文本存档 | advanced_markdown_cleaner |

---

## 🎯 总结

### 核心要点

1. **格式标记有意义** - 它们代表原文的排版结构
2. **智能转换更好** - 保留有用信息,删除冗余标记
3. **目录必须保留** - 提供文档导航
4. **自动化很重要** - 集成到翻译流程中

### 推荐工作流

```
EPUB 文件
   ↓
Pandoc 转换
   ↓
翻译引擎 (async_translator.py)
   ↓
智能格式化 (smart_markdown_formatter.py) ← 推荐!
   ↓
最终 Markdown 文件
```

---

## 📞 常见问题

### Q: 目录链接还能用吗?

A: 目前保留的是原始HTML锚点,在本地Markdown阅读器中可能无法跳转。可以进一步优化为标准Markdown锚点。

### Q: 图片路径修复后还是找不到图片?

A: 确保图片文件在 `images/` 目录中,或使用绝对路径。

### Q: 为什么有些段落还是被错误分割了?

A: 这是原始EPUB格式问题,可能需要手动调整。

### Q: 能自动将所有标记转换为对应的Markdown格式吗?

A: 当前工具已经处理了主要情况。复杂的嵌套结构可能需要手动调整。

---

**更新时间**: 2026-01-16
**工具版本**: v2.0
